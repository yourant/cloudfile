server:
  port: 7001
spring:
  application:
    name: sop-demo-cluster
  # nacos
  cloud:
    nacos:
      discovery:
        enabled: true
        service: ${spring.application.name}
        server-addr: 10.49.0.115:9999
#      config:
#        server-addr: 10.49.0.115:8848
#        shared-configs:
#          - data-id: sop-demo-cluster-dev.yml
#            group: DEFAULT_GROUP
#            refresh: true
