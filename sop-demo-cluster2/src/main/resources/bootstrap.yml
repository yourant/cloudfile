spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        shared-configs:
          - data-id: sop-demo-cluster-dev.yml
            group: DEFAULT_GROUP
            refresh: true